import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles(theme => ({
    fullContainer: {
        height: '100vh',
        width: '100%',
        display:'flex',
        alignItems:'center'
    },
    container:{
        textAlign:'center',
    }
}))
export default useStyles